---
servitus:
  schema_version: 1
  system_version: 0.3.5
  record_type: dev-backlog
  pipeline_stage: captured
  status: in_progress
  intent: design
  authority: derived
date: 2026-06-02
title: "Grok Build + Hermes Harness Usage Strategy"
slug: "grok-build-hermes-usage-strategy"
priority: high
tags: [grok, hermes, launcher, harness, strategy, division-of-labor, tmux, capture, github, contractor]
origin:
  artifact: "grokMarkdownFiles collection — Grok (xAI) voice chats exported via Markdown Chrome plugin from phone. This is the first time the user has ever downloaded such exports onto this Linux machine using the plugins. Previously used the same plugin to sync/export everything from Anthropic/Claude (a lot of voice chats outside of Claude Code), and before that a prior tool to evacuate all OpenAI conversations (Grok/xAI 'really didn't even have an export' at the time). Moved 2026-06-02 from ~/Downloads/grokMarkdownFiles/ (user-provided path) into Inbox/Grok/grokMarkdownFiles/. Primarily voice chat exports (Audio_Conversation.md files, etc.). Key file: 2026-05-31-Obsidian_AI_Second_Brain_Vault.md (heavy on transcribed verbal processing in the knowledge vault, dormant notes from voice input, AI workflows to activate them for decision support/writing/conversation/action — directly matches user's description of exports and the 'issues trying to figure out how to do everything' with the second brain). Other candidates promoted/referenced: 2026-06-01-Servetus_Video.md, various Audio_Conversation.md. See _INGEST-2026-06-02.md in the collection for full details."
  session: "Grok Build TUI on 2026-06-02-grok-build-hermes-usage-strategy branch"
---

# Grok Build + Hermes Harness Usage Strategy

**Origin:** Long Grok voice chat on phone (exported as markdown, not yet in hopper). User: "I've been having a lot of issues trying to figure out how to do everything that I want to do." Explicit intent: absolutely use the Hermes harness; use Grok Build (this contractor engine) to design and build *how* to use it. First deliberate use of GitHub capabilities (gh + git work on branch/PR) from within Grok Build in Servetus context. Started after navigating to vault and `git init` (reinit) to establish proper context.

**Status:** Design + initial implementation in progress on branch `2026-06-02-grok-build-hermes-usage-strategy`. Parallel subagents (explore + plan) spawned in isolated worktrees for gaps analysis and draft. This doc is the canonical living record.

## Plain English Goal
Stop the fragmentation and "figuring out" tax. Have a crisp, documented, launcher-supported model so that:
- Hermes is the reliable daily runtime harness (skills, dispatch, voice, persistent agents, home automation, the "shell").
- Grok Build is the high-power contractor tool used *outside* the harness for design spikes, parallel exploration (subagents + worktrees), precise multi-file work, GitHub/PRs, authoring the specs that then load into Hermes.
- The tmux control room (servitus) is the "everything" meta-view when you want all engines visible at once (video-mixer/ATEM model, user as TD).
- Capture, git, and vault sovereignty are non-negotiable for every path.
- Launching the right thing for the job is one or two commands (servetus, servetus-h, servetus-g, etc.).

## Current State (as of branch creation + 2026-06-01 tmux session)
- **bin/servitus**: Full preflight/onboarding bootstrap. Boots 6-pane tmux "control room" (Servetus/Claude pane top-left, Hermes pane, 4 agent slots). Heavy on deps (Hermes from git, tmux, claude, node, Nextcloud, Obsidian). No -g/-h standalone dispatch yet.
- **Toolkit/servetus-tmux-launcher.sh**: Simpler/hardcoded layout script (labels Control/Hermes/Agents, comments for what to run in panes). Used or evolved into the above.
- **Naming (S00.01-22-00)**: Table and command matrix exist and are close; updated on this branch with exact 2026-06-01 decisions (servetus full tmux+menu+brief+Hermes default; servetus-c/h/g/o/m thin or flag variants; "serve" preserved; sh forbidden). Resume explicitly calls out that the spec "needs update" and lists TODOs for dispatch in ~/bin/servitus.
- **Vision from tmux session summary**: "Hermes is the shell. Claude, Qwen, Grok are engines. The user is the TD (technical director). Panes are camera feeds. You call the shot... Tmux is a textual multiplexer — every pane is a readable/writable buffer the control pane can orchestrate."
- **Related active work**: Live transcript concierge (Otter bridge to web-concierge + live Q&A), voice-xai-orchestration, web-concierge, reporter spec, tmux multiview styling handoff (ATEM-like borders/labels, centering issues), AIO updates, Discord, etc.
- **Grok Build identity** (GROK.md, AGENTS.md, servetus skill): Contractor engine. Plan mode default for complex. Strengths: parallel subagents in worktrees, precise edits, long-horizon, GitHub. Capture via 10-System/grok-session-export.py -> Inbox/Grok/. Recommended launch: servetus-g / sg (sets SERVETUS_VARIANT, cds to vault, starts Grok). "Grok Build is a contractor engine under the Servetus architecture."
- **Hermes side**: Boot skill, servetus profile, jsonl session exports in Toolkit/. Forked for dispatch/skills/HA. Vault is sovereign memory (not Honcho).
- **Capture**: Separate but converging (Claude session-close.py, Grok export.py). Must land raw in Inbox/* then move to 00-Artifacts with provenance.
- **GitHub**: Remote configured (origingit + macbookssh). gh token currently invalid in this env (user to re-auth for full demo).

## Gaps Analysis (detailed report from parallel explore subagent on this branch)

Full exploration (85 tool calls, reads of bin/servitus, Toolkit/servetus-tmux-launcher.sh, naming spec, session-resume, tmux-handoff, GROK.md, AGENTS.md, SKILL.md, 10-System/*, .hermes/profiles, etc.) is captured in the subagent output. Key findings embedded here for the canonical strategy (a local copy of the raw report exists at 10-System/2026-06-02-current-harness-launcher-gaps.md on FS for reference).

**Executive Summary of Current Architecture (from subagent):**
- tmux as "hantainer" (6-pane video-mixer/ATEM control room: Control top-left, Hermes top-right, 4 agent slots bottom). Styling heavy borders + bottom labels (centering broken on tmux 3.4 per handoff.md). User as TD.
- Current launcher (the active `servetus` (no 'i') in ~/bin/): unified MODE parsing for full (Hermes in Control 0.0 via --skills servetus-boot --profile servetus), single -h/-g/-c/-o, preflight dashboard, thin wrappers (servetus-h etc.), always calls session-close.py on single exit. Legacy `servitus` (with 'i') in ~/bin/ is the old preflight-heavy one (still functional).
- Planned (naming + resume decisions, updated on this branch): `servetus` = full tmux+menu+brief+Hermes default; `servetus-h/g/c/o/m` for standalone/attach. "serve" preserved, sh forbidden. Env SERVETUS_VARIANT.
- Hermes: profile `servetus`, `hermes --skills servetus-boot --profile servetus` (svts alias). Voice primitive. No native capture.
- Grok Build: servetus-g/sg, auto export to Inbox/Grok/, plan mode, worktrees, GitHub strengths.
- Capture: asymmetric (Grok export.py, Claude close.py; Hermes relies on generic/Claude tool or manual).
- Citations throughout subagent report (file:line).

**Specific Gaps (5 main, directly from subagent):**
1. Reliable pure Hermes start with Servetus boot/skill? (`servetus-h` calls the right command but servetus-boot skill location/definition unclear in vault; no dedicated Hermes-only preflight/brief that guarantees full constitution + bootloader load order without tmux/Claude assumptions; single mode still calls Claude-oriented session-close.py).
2. Grok Build handoff specs/skills to Hermes? (Grok commits specs like this one or naming updates to Toolkit/; no formalized ritual, no skill reload documented for Hermes pane, no contract that vault changes are visible to Hermes without restart. Bootloader engine-agnostic but Hermes injection sparse.)
3. Capture for both (and unified)? (No dedicated hermes-session-close.py; no unified hook in launchers for all variants; tmux panes lose state; voice sessions need parallel ritual. Post-single always calls session-close regardless of engine. Raw -> hopper -> 00-Artifacts/01-Records per hierarchy.)
4. GitHub/gh from Hermes sessions? (Grok strengths include gh + branches + PRs + worktrees. gh not reliably in Hermes env/PATH/auth scope. No docs or patterns for "from Hermes pane: gh pr create for this spec change". No harness-scoped gh like the secrets-bridge.)
5. Other: launcher variants incomplete (old servitus(i) vs new coexist; dispatch TODOs open; servetus-m not wired; preflight modes differ); Hermes chassis decided in resume but skills not robust; tmux centering broken; division not enforced in practice; voice integration (new voice OS, otter bridge, pop-up concierge) is Servetus primitive but capture/handoff early.

**Recommendations for Division of Labor (from subagent, aligned with existing design doc + GROK.md + resume):**
- **Hermes (runtime/persistent/skills/voice/dispatch/harness)**: Daily driver, agent orchestration, long-running monitors (transcription, live transcript concierge, bot), voice presence (headset primary, barge-in), skill catalog + boot, tmux pane execution, HA integration. Launch: servetus (full) or servetus-h (pure). "The shell". User = TD/Director.
- **Grok Build (design/parallel impl/GitHub/contractor)**: High-leverage spikes, complex refactors, authoring specs that become Hermes skills, parallel subagents/worktrees, precise edits, GitHub ops (branch/commit/gh pr), long-horizon planning. Launch: servetus-g/sg. "Outside the harness" for the building phase. Handoff: commit to vault (Toolkit/ S00.* etc.); Hermes next boot picks up. Always capture raw first.
- **tmux control room (meta / user-as-TD)**: Simultaneous visibility. servetus default. Panes as feeds; Control orchestrates.
- Non-negotiables (all paths): Capture ritual (raw artifact first); vault sovereign (constitution + bootloader first); harness-scoped creds; house style; engine forgets, vault remembers.

**Suggested Next Concrete Steps (7, from subagent, prioritized for this Grok Build contractor session; ties to recent voice exports ingest):**
1. Launcher dispatch + variants (immediate): Reconcile old ~/bin/servitus (i) vs unified; make servetus-h/g/c/o/m reliable (test pure Hermes with full boot/skill + SERVETUS_ROOM); wire servetus-m; add common pre-brief/capture hooks. (Cites: bin/servetus, naming:108, resume:45, wrappers.)
2. Hermes usage model + handoff: Stabilize servetus-boot skill (proper loadable); document pure start; formalize Grok->Hermes handoff (post-Grok ritual or script that notes in resume/radar or triggers reload). Exercise in next Hermes session.
3. Capture unification + Hermes (critical for voice): Create/extend hermes-session-close.py (parallel to grok export + claude close); call from Hermes single + Control/exit; unify; add Voice Session format. Update launchers + tmux quickref. (Directly relevant to the grokMarkdownFiles voice batch we just ingested into Inbox/Grok/voice-exports/ + promoted artifacts: plugin exports need the same ritual as TUI ones.) Started implementation: stub created at 10-System/hermes-session-close.py on branch FS (modeled on the existing exporters; notes the Chrome plugin voice context and future routing to voice-sessions/). (10-System/ items may be .gitignored for new files; value tracked via this doc + will be promoted/committed when the pattern stabilizes or moved to a trackable location.)
4. GitHub/gh + contractor patterns from Hermes: Document/script "gh from Hermes" (PATH + harness-scoped like secrets-bridge); demo gh pr from Hermes-launched spec edit. Add to director-interface skill + cross-refs in GROK.md/AGENTS.md. (This branch is the first deliberate GitHub use from Grok Build in Servetus context.)
5. Docs + cross-refs: Place/integrate this gaps analysis (done via this strategy doc + the 10-System/ local report); update naming (already partially done on branch), resume (Critical Path 0), GROK.md, AGENTS.md, session-start/close, tmux-quickref; add Hermes usage how-to section to Toolkit/.
6. Test + voice integration: End-to-end (servetus full, servetus-h pure, handoff to servetus-g for design, capture, voice via servetus-speak/presence); wire live transcript concierge + otter bridge into Hermes dispatch. (Our recent voice plugin batch + the report's call for voice capture parallel to terminal makes this timely.)
7. Other: Update launch-brief/menu for Hermes-first; fix tmux centering; test on clean machine; distribution after solid.

See the full subagent report content (exec summary, citations, etc.) embedded in the parallel explore output for exhaustive file:line detail. This report + the voice exports context (Chrome plugin, first on Linux, history of Anthropic/OpenAI voice evacuations, primary artifacts are voice chats) directly drive the Hermes usage model we are building with Grok Build.

## Division of Labor (proposed)
- **Hermes Harness (runtime)**: The persistent, skill-driven, dispatch-oriented, voice-first operating environment. Use for: daily tasks, agent orchestration, long-running monitors (transcription queue, live transcript, bot), voice conversations, home automation, anything that benefits from the boot skill + vault context + skill catalog. Launch via servetus (full room) or servetus-h (quick single).
- **Grok Build (contractor)**: High-leverage, stateless-per-session, planning + execution engine. Use for: designing new systems (this work), complex refactors, parallel investigation (multiple subagents), authoring specs that become Hermes skills, GitHub work (branches, PRs, reviews), anything requiring worktrees or heavy editing. Launch via servetus-g. "Outside the harness" by design for the building phase.
- **Tmux Control Room (meta)**: When you want simultaneous visibility and orchestration across engines (user as TD calling shots via Control pane). The "hantainer". servetus launches it.
- **Other engines** (Claude Code direct, Ollama): Via -c / -o for specific precision or bulk needs.

Handoff: Grok Build session ends with committed changes to vault (specs, code, docs). Hermes next launch (or skill reload) picks them up. Raw transcripts/artifacts always captured back to vault first.

## Launcher Family (to implement)
See updated naming spec. Thin wrappers or arg dispatch in bin/servitus (or ~/bin/servetus-* symlinks/scripts) so:
- `servetus` or `servitus` -> full tmux room (Hermes in control per resume vision).
- `servetus-h` or `servetus --hermes` -> clean Hermes + Servetus boot skill, no tmux.
- `servetus-g` or `servetus --grok` -> this Grok Build TUI, with SERVETUS_VARIANT set, proper vault cd, capture on exit.
- `servetus-c`, `-o`, `-m` similarly.
- Pre-flight, brief, capture hooks common.

Also support env var override and room name (SERVETUS_ROOM).

## Capture & Governance (non-negotiable)
Every path must run the ritual: raw to Inbox/Grok or Inbox/Hermes (or GrokVoice for phone exports), then processing to 00-Artifacts (timecoded, witnessed), 01-Records, etc. Use/extend the existing export.py and session-close.py. No durable state left in frontier engines. Hierarchy: artifact wins.

This session itself will be captured via 10-System/grok-session-export.py at close.

## GitHub Capabilities (first use demo)
- Local git on feature branch for all strategy work.
- Commit often with clear messages.
- Push to origin.
- Use gh (after re-auth) for: gh pr create, gh issue comment, viewing Servetus repo, perhaps creating tracking issue for the strategy.
- Safe parallel via worktrees (this subagent setup is example).
- Goal: make GitHub ops feel natural from both Grok Build and Hermes contexts (e.g. gh available in panes, scripts that open PRs for spec changes).

## Next Steps (this session + follow; updated with subagent 7 steps + voice context)
See the detailed 7 concrete next steps (with citations) in the **Gaps Analysis** section above (from the completed parallel explore subagent). They are the actionable plan for this Grok Build contractor session.

Priorities for immediate follow (integrating the recent voice exports ingest):
1. Launcher dispatch + variants (make servetus-h/g reliable for pure Hermes with full boot/skill and for Grok Build; wire -m; common hooks).
3. Capture unification + Hermes (create hermes-session-close.py parallel to grok export; handle plugin voice exports like the grokMarkdownFiles batch now in Inbox/Grok/voice-exports/; unify for all variants including voice sessions).
2/5/6. Handoff formalization, docs/cross-refs (this strategy doc + gaps report, update resume/naming/GROK.md/AGENTS.md), test + voice integration (end-to-end with the voice plugin artifacts we just ingested).

7. Other from subagent.

Continue monitoring the plan subagent for its draft/implementation plan output.

Then: commit/push (gh after auth), capture this session, exercise in subsequent Hermes/tmux sessions.

## Open Questions (to resolve in design)
- Exact control pane default (Hermes vs Claude) in full servitus?
- How does a Hermes session "call out" to Grok Build for a heavy design task (spawn TUI in a pane or external)?
- Skill vs subagent boundary (what lives in Hermes skills catalog vs what Grok Build does transiently)?
- Stream Deck / voice / physical invocation of specific variants?
- Multi-user / federation implications for the model?

See also: session-resume.md (Critical Path 0 + 4), tmux-multiview-handoff, the originating phone export (pending), constitution (map vs engine, renter role, capture ritual), bootloader (load order).

This is the floor. The vault remembers; engines are temporary.

---
*Created 2026-06-02 on Grok Build feature branch as the canonical artifact for this design effort.*