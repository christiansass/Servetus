# Nextcloud adapter dependencies
# All run locally, no telemetry, standard licenses
requests>=2.28.0     # Apache-2.0 — HTTP client for WebDAV + Talk API
pyyaml>=6.0          # MIT — config file parsing
