# Servetus Voice Pipeline Dependencies
#
# Install: pip install -r requirements-voice.txt
#
# All packages run locally. No network required after install.
# No telemetry. No phone-home. Your audio stays on your device.

# Audio capture and file I/O
sounddevice>=0.4.6      # Record from microphone (MIT)
soundfile>=0.12.1       # Read/write WAV files (BSD)
numpy>=1.24.0           # Signal processing (BSD)

# Transcription (local Whisper)
faster-whisper>=1.0.0   # CTranslate2 Whisper — fast local transcription (MIT)

# Prosodic analysis (optional but recommended)
parselmouth>=0.4.3      # Praat wrapper for pitch/prosody analysis (GPL-3.0)
